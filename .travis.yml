language: dart
dart:
  # Install the latest stable release
  - stable

install:
  - gem install coveralls-lcov

dart_task:
  - dartfmt: false
  - dartanalyzer: true
  - test: --platform vm
os:
  - linux
#  - osx
  - windows

after_success:
  - coveralls-lcov coverage/lcov.info
